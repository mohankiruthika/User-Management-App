import React from 'react'

const About = () => {
  return (
    <div>
        <h1>User Management App</h1>
        <p>This is a User Management application built using React. It allows users to manage a list of users, view details, add, and remove users, among other features.</p>
    </div>
  )
}

export default About